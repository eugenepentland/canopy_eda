;; Bare 1 mm probe pad. A board feature rather than a purchased part, so it
;; carries no manufacturer, MPN or datasheet.
(component "testpoint"
  (description "1 mm SMD test point / probe pad")
  (pinout testpoint)
  (footprint testpoint-1mm)
  (refdes "TP")
  (ignore-requirements))
